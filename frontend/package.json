{
  "name": "jamdna-frontend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "backend": "cd ../backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "fullstack": "concurrently \"npm run backend\" \"npm run dev\"",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "concurrently": "^8.2.2",
    "typescript": "^5.4.0",
    "vite": "^7.1.8"
  }
}
